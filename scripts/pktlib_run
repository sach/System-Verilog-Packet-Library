#/bin/bash
vcs -sverilog -full64 -f pktlib.vf -R test/$1.sv +define+NO_PROCESS_AE -l log/$1.log 

#vlib work
##vlog -64 -sv -permissive -timescale "1ns/1ps" +define+NO_PROCESS_AE -f pktlib.vf test/$1.sv
##vsim -64  -do "run  -a; quit

